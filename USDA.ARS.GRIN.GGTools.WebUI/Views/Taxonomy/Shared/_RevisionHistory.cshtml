@if (Model.Entity.ID > 0)
{
    <div class="row">
        <div class="col-md-12">
            <div class="form-group">
                @Html.Raw(Model.Entity.RevisionHistoryText)
            </div>
            </div>
    </div>
}
