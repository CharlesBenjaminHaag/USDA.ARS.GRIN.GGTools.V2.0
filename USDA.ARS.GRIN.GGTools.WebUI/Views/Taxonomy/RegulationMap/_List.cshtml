@model USDA.ARS.GRIN.GGTools.Taxonomy.ViewModelLayer.RegulationMapViewModel
<div id="section-search-results" class="card card-default">
    <div class="card-header">
        <h3 class="card-title">Regulation Maps (@Model.DataCollection.Count)</h3>
        <div class="card-tools">
            <button type="button" class="btn btn-tool" data-card-widget="collapse">
                <i class="fas fa-minus"></i>
            </button>
        </div>
    </div>
    <div class="card-body">
        <table id="data_table_taxonomy_regulation_map" class="table table-striped ggtools-table" style=" width:100%;">
            <thead>
                <tr>
                    <th></th>
                    <th width="5%">ID</th>
                    <th>Assembled Name</th>
                    <th>Genus</th>
                    <th>Species</th>
                    <th>Exempt</th>
                    <th>Note</th>
                    <th>Last Modified</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @if (Model.DataCollection != null)
                {
                    if (Model.DataCollection.Count() > 0)
                    {
                        foreach (var result in Model.DataCollection)
                        {
                        <tr>
                            <td>
                                @result.ID
                            </td>
                            <td>
                                @result.ID
                            </td>
                            <td>@result.AssembledName</td>
                            <td>
                                <a href="@Url.Action("Edit", "Genus", new { entityId = result.GenusID })">
                                    @result.GenusName
                                </a>
                            </td>
                            <td>
                                <a href="@Url.Action("Edit", "Species", new { entityId = result.SpeciesID })">
                                    @result.SpeciesName
                                </a>
                            </td>
                            <td>
                                @Html.Raw(Model.FormatBoolean(result.IsExempt))
                            </td>
                            <td>@Html.Raw(result.Note)</td>
                            <td>
                                @if (result.ModifiedDate != DateTime.MinValue)
                                {
                                    @result.ModifiedDate.ToShortDateString()
                                }
                            </td>
                            <td>
                                <a href="@Url.Action("Edit", "RegulationMap", new { entityId = result.ID })" class="btn btn-primary btn-sm pull-right row-edit">
                                    <i class="fa fa-edit"></i>
                                </a>
                                @*@if (Model.Action == "Folder")
                                {
                                    <button id="btnDeleteFolderItem_@cropForCWR.ReferenceID" class="btn btn-danger btn-sm pull-right row-edit margin-r-5">
                                        <i class="fa fa-trash"></i> Remove From Folder
                                    </button>
                                }*@
                            </td>
                        </tr>
                        }
                    }
                }
            </tbody>
        </table>
    </div>
</div>
<script type="text/javascript">
    $(document).ready(function () {
        InitDataTable("data_table_taxonomy_regulation_map");
    });
</script>
