@model USDA.ARS.GRIN.GGTools.Taxonomy.ViewModelLayer.GeographyViewModel
<button id="btnLoadAdminstrativeUnits" type="button" class="btn btn-primary btn-sm btn-block">Load Admin Units >></button>
<br />
<table id="data-table-geography-countries" class="table table-striped ggtools" style="width:100%">
    <thead>
        <tr>
            <th></th>
            <th>Name</th>
        </tr>
    </thead>
    <tbody>
        @if (Model.DataCollectionCountries.Count() > 0)
        {
            foreach (var result in Model.DataCollectionCountries)
            {
                <tr>
                    <td>
                        @result.CountryCode
                    </td>
                    <td>@result.CountryDescription</td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="3">
                    <div class="callout callout-info" style="vertical-align:middle; margin-top:5px;">
                        Your search returned no matches.
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>
<script type="text/javascript">
    $(document).ready(function () {
        InitDataTableGeo("data-table-geography-countries");
    });
</script>
